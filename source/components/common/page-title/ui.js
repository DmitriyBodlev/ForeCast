import { css } from 'react-emotion';

const wrapper = css`
  font-size: 20px;
  font-weight: 500;
  height: 60px;
  line-height: 60px;
  padding-left: 15px;

  background-color: white;
  color: black;
`;

const styles = {
  wrapper,
};

export default styles;
