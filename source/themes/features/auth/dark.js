import colors from '../../colors';

export default {
  forms: {
    bgColor: colors.dark.mainDark,
    titleColor: colors.white,
    inputs: {
      borderColor: colors.dark.lightGrey,
      borderColorErr: colors.dark.red,
      boxShadow: '0 0 5px 2px rgba(206, 40, 40, 0.5)',
    },
    buttons: {
      bgColor: colors.dark.mainRed,
      textColor: colors.white,
    },
    labels: {
      color: colors.white,
    },
  },
};
